info:
  name: Delete URLs
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/api/urls/YOUR_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
