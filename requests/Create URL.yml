info:
  name: Create URL
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/urls"
  body:
    type: json
    data: |-
      {
        "originalUrl": "https://google.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
