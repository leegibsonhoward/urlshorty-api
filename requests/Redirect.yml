info:
  name: Redirect
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/YOUR_SHORT_CODE"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
